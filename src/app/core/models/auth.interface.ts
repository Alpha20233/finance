export interface icon {
  name: string;
  type?: string;
}

export interface validCond {
  cond1: boolean;
  cond2?: boolean;
  form_submit: boolean;
}

export interface signIn {
  cEmail: boolean;
  cPass: string;
}
